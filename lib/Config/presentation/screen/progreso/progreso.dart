import 'package:flutter/material.dart';

class Progreso extends StatelessWidget {
  static const String name = 'progreso';
  const new({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}